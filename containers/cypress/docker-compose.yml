networks:
  default:
    name: elabftw-net
services:
  cypress:
    image: elabftw/elab-cypress:15.20.0
    extra_hosts:
      - "elab.local:host-gateway"
    build:
      context: ../..
      dockerfile: ./containers/cypress/Dockerfile
    container_name: elab-cypress
