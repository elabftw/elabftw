networks:
    elabftw-net:
services:
  cypress:
    image: elab-cypress
    extra_hosts:
      - "elab.local:host-gateway"
    build:
      context: ..
      dockerfile: ./tests/elab-cypress.Dockerfile
    container_name: elab-cypress
    networks:
      - elabftw-net
