actor: Apiv2Tester
coverage:
  enabled: false # disabled until the c3 stuff can be figured out
modules:
  enabled:
        - \Helper\Api
        - REST:
            url: https://elabtmp/api/v2/
            depends: PhpBrowser
