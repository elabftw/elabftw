/////// Priority
users should be validated immeditely if config = 0
* possibility to add several templates (block of templates)
* export by date range / tag
* search : selector should stay what it was
* search : multiple critères( has file attached ?)
* most used protocoles get first in selecta ! 

////// Features
* announce birthday date of a team mb to other team mb (add birthday in profile)
* labmeetings : resp can add room, date, name, cronjob that sends an email before the labmeeting
* when click on file div, change file (with ajax ?)
* add real calendar
* custom menu

///// Osef
* have a real useful index...
* merge experiments_tags and protocols_tags
* It's impossible to make a date before 2010 or after 2099
* selected outcome is not kept if error on edit/create
* if two file field and file is only on the second one (first is empty) no file uploaded

///// Longterm
* install.php
* music manager à la ledjamradio
* autosuggest on tags (supplanted by browser form history)
* i18n
* offline version 

///// SECURITY
* check for flood register/login
